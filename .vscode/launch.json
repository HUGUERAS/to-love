{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    
    "version": "0.2.0",
    // Debug configurations array for Visual Studio Code.
    // Each item is a debug configuration object that defines how to launch or attach
    // to an application (common fields include: name, type, request, program, cwd,
    // args, env, preLaunchTask, and other debugger-specific settings).
    // Add or edit configurations to enable different run/debug scenarios; select them
    // from the Run and Debug view. For full schema and examples, see:
    // https://code.visualstudio.com/docs/editor/debugging
    "configurations": [

        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Edge against localhost",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}